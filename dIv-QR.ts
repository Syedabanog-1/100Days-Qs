function QR(a: number= 22, b: number=10) : any{

    const findoutQR: any= {
        a: 22,
        b: 10,
}
return console.log("Qoutient is:", Math.floor(a/b), "Reminder is:", a % b);
}

QR();
