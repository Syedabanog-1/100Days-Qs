//Area of Rectangle and doing Refactored using areo function
 
 
 let AreaOfRec = (width: number=5,hight: number=12 , sentence: string = "Calculate Area Of Rectangle using Aero Function:") =>  
                  console.log(sentence, width * hight);
AreaOfRec();
 
   function rec_Area(width: number=10, hight: number = 20, sentence: string= "Calculate Area Of Rectangle using Tradition Function:"): void {

   const A_Rec: any = console.log( sentence, width * hight);
   return A_Rec;

}
  rec_Area();
  rec_Area(30,45);

 

