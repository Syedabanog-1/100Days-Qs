var curArray = [1, 92, 110, 5, 12, 14];
var doubleNum = curArray.map(function (numD) {
    return numD * 2;
});
console.log("Current Number: ", curArray);
console.log(" Number are double in new Array: ", doubleNum);
