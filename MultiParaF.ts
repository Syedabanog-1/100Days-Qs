

function fav_hobbies(hobby: string ='gardening:' ,
  desc: string ='watering', place: string='own home garden',Tdetail: string= '2mor-eve')
  {
       return console.log(`\tHobby: ${hobby}, \n\tdescription: ${desc}, \n\tPlace: ${place} \n\tTime Detail: ${Tdetail}`);
      
  };
  // call the function
  fav_hobbies ();
  //call funtion for size medium
  fav_hobbies("Swimming",);
// call function for small size
fav_hobbies('Travelling','when get free','anywhere i liuke','in my spare timeon  weekend');